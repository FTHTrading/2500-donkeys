node_modules/
.env
*.log
.DS_Store
Thumbs.db

# Build outputs
dist/

# Generated images — PNGs tracked in git because their SHA-256
# hashes are part of the on-chain Merkle tree (Polygon).
# Only generation-log.json is ignored.
images/generation-log.json

# Generated audio (large MP3s — tracked via IAPL-1 manifest, not binary)
audio/rendered/
audio/rendered-stories/

# Site backup files (development artifacts)
site/*-backup.*

# Hardhat
web3/cache/
web3/artifacts/
cache/
typechain-types/
